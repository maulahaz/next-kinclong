# Skill: Create Drizzle Schema

## Rules
- Use UUID as primary key
- Include createdAt

## Relationships
- Use foreign keys
- Normalize data

## Example
- users → customers → cars → contracts → washes